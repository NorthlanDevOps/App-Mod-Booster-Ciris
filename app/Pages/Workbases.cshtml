@page
@model WorkbasesModel
@{
    ViewData["Title"] = "Workbases";
}

@if (Model.ErrorInfo != null)
{
    <div class="error-banner">
        <strong>⚠️ Database Connection Error:</strong><br/>
        @Model.ErrorInfo.Message
    </div>
}

<div class="page-header">
    <h1>Workbases</h1>
</div>

<div class="table-container">
    <table class="data-table">
        <thead>
            <tr>
                <th>Workbase ID</th>
                <th>Workbase Name</th>
                <th>Address 1</th>
                <th>Address 2</th>
                <th>Postcode</th>
                <th>Telephone</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var workbase in Model.Workbases)
            {
                <tr>
                    <td>@workbase.WorkBaseID</td>
                    <td>@workbase.SectionDescription</td>
                    <td>@workbase.PropertyName</td>
                    <td>@workbase.SectionID</td>
                    <td>@workbase.PropertyID</td>
                    <td></td>
                </tr>
            }
        </tbody>
    </table>
</div>
